<div class="col-md-12">
  <div class="group">
    <%= link_to("Edit", edit_group_path(@group),class: "btn btn-primary pull-right")%>
    <%= link_to("Delete", group_path(@group),class: "btn btn-primary pull-right",
                method: :delete, data: {confirm:"are you sure?"})%>
  </div>
  <h2><%= @group.title %></h2>
  <p><%= @group.description %></p>
</div>
